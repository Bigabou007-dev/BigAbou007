# Copy to .env and fill in. Never commit .env.
DB_ROOT_PASSWORD=change_me_root
DB_NAME=keystone
DB_USER=keystone
DB_PASSWORD=change_me_app

# Cfx server license key — get one free at https://keymaster.fivem.net
# Set this in server-data/server.cfg (sv_licenseKey) after generating it.
